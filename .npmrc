; pnpm configuration for this single-package repo.
; Use pnpm as the canonical package manager for external clones / CI.
; (Lovable's internal build harness uses package-lock.json — both are supported.)

auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
resolution-mode=highest
save-exact=false
engine-strict=false
